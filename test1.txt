Гусев Дмитрий


#include<stdlib.h>//подключение латиницы
#include<iostream>//подключение ввода и вывода
using namespace::std;
void main()/*Предусловие:в двумерынй массив записаны названия городов Постусловие:Подсчитать количество названий, которые оканчиваются буквой «В».*/
{
	system("chcp 1251");
	int k = 0;
	char st[10][100];//задание двумерного массива
	puts("Введите города через пробел: ");
	for (int i = 0; i < 10; i++)//цикл для записи массива
	{
		cin >> (st[i]);//запись массива
	}
	char s, n;
	k = 0;
	n = 'q';
	for (int i = 0; i < 10; i++)//цикл для строк массива
		for (int j = 0; j < 100; j++)//цикл для столбцов массива
		{
			if (st[i][j] == '\0')//поиск конца строки
				n = st[i][j - 1];
			if (n == 'в')//условие счёта
			{
				k++;
				n = 'q';//"обнуление" для подсчёта
			}
		}
	cout << "Кол-во городов, название которых оканчивается на В:" << k  << endl;
}
